"use client";
import React from "react";
import "@/src/styles/v0-scope.css";
export function V0Section({ children }: { children: React.ReactNode }) {
  return <section className="v0-scope">{children}</section>;
}
